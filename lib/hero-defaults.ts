/** Fallback homepage hero when no image is set in Homepage Settings (matches `--color-stone-white`). */
export const HERO_DEFAULT_BACKGROUND = "#F9F9F8";

/** Studio pane preview placeholder when no hero image is uploaded yet. */
export const HERO_DEFAULT_BACKGROUND_GRADIENT =
  "linear-gradient(135deg, #F9F9F8 0%, #EDEBE5 100%)";

export const HERO_JPEG_HINT = "Use JPEG for photos";
