/** Shared scroll-reveal motion tokens — single source of truth for all marketing pages. */
export const PAGE_REVEAL_STAGGER = 0.04;
export const PAGE_REVEAL_DURATION = 0.65;
export const PAGE_REVEAL_OFFSET_Y = 16;
export const PAGE_REVEAL_EASE = [0.22, 1, 0.36, 1] as const;
export const PAGE_REVEAL_VIEWPORT = { once: true, amount: "some" } as const;
